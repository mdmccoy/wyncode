require './election'

describe Election do
  context "When a person is created" do
    context "when an error is raised" do
      #this doesn't work right now...
      # it "should puts to the screen" do
      #   Election.new.create
      #   STDOUT.should_receive(:puts).with("Please enter a valid name and party")
      #end
    end
  end
end
